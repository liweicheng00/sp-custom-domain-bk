## Setup

```bash
pip install -r requirements.txt
```
## Local Run

Update necessary secrets in `.env`.
### Docker-compose
```bash
docker build -t backend .
docker-compose up
```

### Run in venv
```
python run.py
```
